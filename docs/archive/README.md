# Archived Documents

This directory stores historical planning and research artifacts that are no longer active execution documents.

## Rules

- Active planning/docs live under `docs/adr`, `docs/architecture`, `docs/plans`, `docs/research`, and `docs/releases`.
- Files moved here are preserved for traceability only.
- Archived files must not be treated as current implementation source-of-truth unless explicitly reactivated.
