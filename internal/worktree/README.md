# internal/worktree

Status: Legacy-track package (currently not wired in main runtime path).

Notes:
- Worktree feature is active in the product, but primarily implemented via `internal/git`, `internal/plugins/git`, and app/store command paths.
- This package is retained for compatibility/history during cleanup.
- Do not remove without a dedicated compatibility batch.
