# internal/a2a

Status: Frozen candidate (not wired in current main runtime path).

Notes:
- Keep for historical context and potential protocol fallback analysis.
- Do not add new feature work here unless explicitly reactivated.
- Active orchestration/runtime path should be validated from `internal/mcp`, `internal/orchestrator`, and `internal/app`.
